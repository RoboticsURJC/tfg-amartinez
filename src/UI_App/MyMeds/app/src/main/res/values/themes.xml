<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema REAL de la aplicación -->
    <style name="Theme.MyMeds" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>

        <item name="android:statusBarColor">@color/purple_200</item>
    </style>

    <!-- Tema SPLASH -->
    <style name="Theme.MyMeds.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/purple_very_light</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo_mymeds</item>

        <!-- Tema real tras la splash -->
        <item name="postSplashScreenTheme">@style/Theme.MyMeds</item>
    </style>

</resources>
